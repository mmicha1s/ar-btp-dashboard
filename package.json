{
  "name": "btp",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^10",
    "@sap/cds-dk": "^10",
    "@cap-js/sqlite": "^3"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "url": "db.sqlite" }
      },
      "auth": {
        "kind": "mocked"
      }
    }
  }
}
